# Server Configuration
PORT=5000
NODE_ENV=development

# Database - Choose one option:
# Option 1: Local MongoDB (if you have MongoDB installed)
# MONGODB_URI=mongodb://localhost:27017/bementor

# Option 2: MongoDB Atlas (cloud database - recommended)
MONGODB_URI=mongodb+srv://mripirtskhelani:hollow21@cluster0.qz30ty2.mongodb.net/bementor?retryWrites=true&w=majority&appName=Cluster0

# Option 3: For testing without MongoDB (in-memory)
# MONGODB_URI=mongodb://localhost:27017/bementor

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=bementor-super-secret-jwt-key-2024-change-this-in-production
JWT_EXPIRE=7d

# Email Configuration (optional for now)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000 